<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="/logofundo.jpg" type="image/x-icon">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <title>Reportes</title>
</head>
<body class="bg-light">
    <header class="text-center py-3 bg-white shadow-sm">
        <img src="/logo-grande.png" class="img-fluid" style=" max-height: 100px;" alt="">
    </header>
    <main id="main" class="container my-5">
        <section id="home">
            <div class="main-container row g-4">
                <div class="main-container-1 col-lg-6">
                    <div class="card shadow-sm p-4">
                        <h2 class="text-primary mb-4">Acompanhe seu Reporte</h2>
                        <form action="/page_consult_report" method="post">
                            <div class="forms-container">        
                                <div class="form-consult mb-3">
                                    <label for="" class="form-label fw-semibold">Coloque o Número de Protocolo</label>
                                    <input type="text" class="form-control" name="protocolo_consultado" placeholder="Insira o número de protocolo" required>
                                </div>
                                <div class="button-consult text-end">
                                    <button type="submit" class="btn btn-primary">Consultar</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
                <div class="main-container-2 col-lg-6">
                    <div class="card shadow-sm p-4">
                        <% if (consult_task) { %>
                            <div class="mb-2">
                                <label class="fw-semibold">N° Protocolo: </label> <%= consult_task.protocol %>
                            </div>
                            <div class="mb-2">
                                <label class="fw-semibold">Data do Reporte: </label> <%= consult_task.data.toLocaleDateString('pt-BR', {timeZone: 'America/Sao_Paulo', hour12: false, year: 'numeric', month: 'numeric', day: 'numeric', hour: 'numeric', minute: 'numeric', second: 'numeric'}) %>
                            </div>
                            <div class="mb-2">
                                <label class="fw-semibold">Status: </label> <span class="badge bg-info text-dark"><%= consult_task.etapa %></span>   
                                <canvas id="etapasChart" data-etapa="<%= consult_task.etapa %>" style="max-height: 100px;"></canvas>
                            </div>                             
                            <div class="mt-2">
                                <p class="fw-semibold">Sobre:</p>
                                <div class="alert alert-secondary">
                                    <% if (consult_task.etapa === "Solicitação Reportada") { %>
                                        Sua solicitação foi reportada com sucesso. Aguarde a análise da nossa equipe.
                                    <% } else if (consult_task.etapa === "Solicitação em Análise") { %>
                                        Sua solicitação está em análise. Em breve, a equipe entrará em contato com você.
                                    <% } else if (consult_task.etapa === "Homologação") { %>
                                        A solicitação foi aprovada e está sendo testada em ambiente de homologação.
                                    <% } else if (consult_task.etapa === "Produção") { %>
                                        Sua solicitação foi concluída e está em produção.
                                    <% } else { %>
                                        Acompanhe as atualizações do seu reporte.
                                    <% } %>
                                </div>
                            </div>
                        <% } else { %>
                        <div class="alert alert-danger">Protocolo não encontrado. Por favor, verifique o número.</div>
                        <% } %>
                    </div>
                </div>
            </div>
        </section>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-plugin-datalabels"></script>
    <script src="/script.js"></script>
</body>
</html>